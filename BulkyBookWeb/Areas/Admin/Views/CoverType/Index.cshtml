@model IEnumerable<CoverType>

@{
    ViewData["Title"] = "Index";
}


<div class="container p-3">
    <div class="row pt-4 mb-2">
        <div class="col-6">
            <h2 class="text-primary">Listă tipuri de copertă</h2>
        </div>
        <div class="col-6 d-flex align-items-center justify-content-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary">
              <i class="bi bi-plus-circle"></i>  Crează tip de copertă
            </a>
        </div>
    </div>


    <table class="table table-bordered table-table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    Numele tipului de copertă
                </th>
                <th>
                    Ordinea de afișare
                </th>
                <th>

                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td width="50%">
                        @obj.Name
                    </td>                   
                    <td>
                        <div class="w-75 btn-group" role="group">
                           <a class="btn btn-primary" asp-controller="CoverType" asp-action="Edit" asp-route-id="@obj.Id"> <i class="bi bi-pen"></i> Editează</a>
                           <a class="btn btn-danger" asp-controller="CoverType" asp-action="Delete" asp-route-id="@obj.Id"> <i class="bi bi-trash-fill"></i>Șterge</a>
                       </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>
